name: Reusable E2E Smoke

on:
  workflow_call:
    inputs:
      dotnet-version:
        description: .NET SDK version used for E2E backend setup
        required: false
        default: "8.0.x"
        type: string
      node-version:
        description: Node.js version used for E2E frontend setup
        required: false
        default: "24.13.1"
        type: string

permissions:
  contents: read

env:
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  e2e-smoke:
    name: E2E Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 35
    container:
      image: mcr.microsoft.com/playwright:v1.63.0-noble
    env:
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
          cache: true
          cache-dependency-path: |
            backend/Taskdeck.sln
            backend/**/*.csproj
            backend/Directory.Packages.props

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
          cache-dependency-path: frontend/taskdeck-web/package-lock.json

      - name: Restore backend
        run: dotnet restore backend/Taskdeck.sln

      - name: Install frontend dependencies
        working-directory: frontend/taskdeck-web
        run: npm ci

      - name: Verify Playwright container browser
        timeout-minutes: 1
        working-directory: frontend/taskdeck-web
        run: node -e "const { existsSync } = require('node:fs'); const { chromium } = require('@playwright/test'); const executable = chromium.executablePath(); if (!existsSync(executable)) throw new Error('Missing Playwright Chromium at ' + executable); console.log(executable);"

      - name: Remove stale E2E database
        working-directory: frontend/taskdeck-web
        run: node -e "require('fs').rmSync('taskdeck.e2e.ci.db',{force:true});"

      - name: Run Playwright smoke tests
        timeout-minutes: 12
        working-directory: frontend/taskdeck-web
        env:
          CI: "true"
          TASKDECK_E2E_DB: taskdeck.e2e.ci.db
          TASKDECK_RUN_DEMO: "0"
        run: npx playwright test --project=chromium --reporter=line

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report
          path: frontend/taskdeck-web/playwright-report
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Playwright test results
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-test-results
          path: frontend/taskdeck-web/test-results
          if-no-files-found: ignore
          retention-days: 14
