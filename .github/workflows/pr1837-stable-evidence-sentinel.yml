name: PR 1837 stabilize absent evidence links

on:
  push:
    branches:
      - fix/1837-stable-evidence-sentinel
    paths:
      - .github/workflows/pr1837-stable-evidence-sentinel.yml

permissions:
  contents: write

jobs:
  implement:
    if: github.repository == 'Chris0Jeky/Taskdeck'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/1837-stable-evidence-sentinel
          fetch-depth: 0

      - name: Add the provided-to-absent regression first
        shell: python
        run: |
          from pathlib import Path

          path = Path('frontend/taskdeck-web/src/tests/views/paper/review/ReviewProvenance.spec.ts')
          source = path.read_text(encoding='utf-8')

          old_import = "import type { ProvenanceRow } from '../../../../composables/usePaperReviewSelectors'"
          new_import = """import {
            EMPTY_EVIDENCE_LINKS,
            type ProvenanceRow,
          } from '../../../../composables/usePaperReviewSelectors'"""
          if source.count(old_import) != 1:
              raise SystemExit('expected one selectors type import')
          source = source.replace(old_import, new_import)

          old_tail = """    await wrapper.setProps({ detailsExpanded: true })
              expect(drawer()).toBe(providedReference)
              wrapper.unmount()
"""
          new_tail = """    await wrapper.setProps({ detailsExpanded: true })
              expect(drawer()).toBe(providedReference)

              await wrapper.setProps({ evidenceLinks: undefined })
              const absentReference = drawer()
              expect(toRaw(absentReference)).toBe(EMPTY_EVIDENCE_LINKS)
              expect(Object.isFrozen(toRaw(absentReference))).toBe(true)

              await wrapper.setProps({ metadata: DETERMINISTIC })
              expect(drawer()).toBe(absentReference)
              wrapper.unmount()
"""
          if source.count(old_tail) != 1:
              raise SystemExit('expected one stable-reference test tail')
          path.write_text(source.replace(old_tail, new_tail), encoding='utf-8')

      - name: Commit the red contract
        shell: bash
        run: |
          set -euo pipefail
          git add frontend/taskdeck-web/src/tests/views/paper/review/ReviewProvenance.spec.ts
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "test(review): cover evidence links returning to absent"

      - name: Prove the new contract fails before implementation
        working-directory: frontend/taskdeck-web
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          set +e
          npx vitest run src/tests/views/paper/review/ReviewProvenance.spec.ts
          status=$?
          set -e
          if [ "$status" -eq 0 ]; then
            echo "Expected the new contract to fail before exporting the canonical sentinel."
            exit 1
          fi

      - name: Reuse the frozen module-level sentinel
        shell: python
        run: |
          from pathlib import Path

          selectors = Path('frontend/taskdeck-web/src/composables/usePaperReviewSelectors.ts')
          source = selectors.read_text(encoding='utf-8')
          old = "const EMPTY_EVIDENCE_LINKS: EvidenceLink[] = Object.freeze([] as EvidenceLink[]) as EvidenceLink[]"
          new = "export const EMPTY_EVIDENCE_LINKS: EvidenceLink[] = Object.freeze([] as EvidenceLink[]) as EvidenceLink[]"
          if source.count(old) != 1:
              raise SystemExit('expected one private evidence sentinel')
          selectors.write_text(source.replace(old, new), encoding='utf-8')

          component = Path('frontend/taskdeck-web/src/views/paper/review/ReviewProvenance.vue')
          source = component.read_text(encoding='utf-8')
          old_import = "import type { ProvenanceRow, ProvenanceWeight } from '../../../composables/usePaperReviewSelectors'"
          new_import = """import {
            EMPTY_EVIDENCE_LINKS,
            type EvidenceLink,
            type ProvenanceRow,
            type ProvenanceWeight,
          } from '../../../composables/usePaperReviewSelectors'"""
          if source.count(old_import) != 1:
              raise SystemExit('expected one ReviewProvenance selectors import')
          source = source.replace(old_import, new_import)

          old_drawer_import = "import type { ProvenanceMetadata, EvidenceLink } from '../../../components/review/ProvenanceDrawer.vue'"
          new_drawer_import = "import type { ProvenanceMetadata } from '../../../components/review/ProvenanceDrawer.vue'"
          if source.count(old_drawer_import) != 1:
              raise SystemExit('expected one ReviewProvenance drawer type import')
          source = source.replace(old_drawer_import, new_drawer_import)

          local = "\nconst EMPTY_EVIDENCE_LINKS: EvidenceLink[] = []\n"
          if source.count(local) != 1:
              raise SystemExit('expected one per-instance evidence sentinel')
          component.write_text(source.replace(local, ''), encoding='utf-8')

      - name: Verify the focused provenance contract
        working-directory: frontend/taskdeck-web
        run: |
          npx vitest run src/tests/views/paper/review/ReviewProvenance.spec.ts
          npm run type-check

      - name: Publish implementation and remove this workflow
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/pr1837-stable-evidence-sentinel.yml
          git add frontend/taskdeck-web/src/composables/usePaperReviewSelectors.ts \
            frontend/taskdeck-web/src/views/paper/review/ReviewProvenance.vue
          git diff --cached --check
          git commit -m "fix(review): reuse frozen evidence sentinel"
          git push origin HEAD:fix/1837-stable-evidence-sentinel
