# Dependabot configuration for automated dependency updates
# Policy: docs/ops/DEPENDENCY_UPDATE_POLICY.md
# Linked issue: #148 (OPS-18)

version: 2

updates:
  # --- NuGet (backend .NET) ---
  - package-ecosystem: nuget
    directory: /backend
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: America/Chicago
    open-pull-requests-limit: 10
    reviewers:
      - jekyt
    labels:
      - dependencies
      - backend
    groups:
      # Group minor/patch updates together to reduce PR noise
      dotnet-minor-patch:
        update-types:
          - minor
          - patch
    # Let other major versions come as individual PRs for explicit review.
    ignore:
      # Keep the three RUNTIME EF Core packages mutually consistent on the 8.x
      # line (see #760/#767). Dependabot repeatedly proposes EF Core 9.x for the
      # main package while the Sqlite/Design providers stay on 8.x, which desyncs
      # them and reintroduces the ambiguous ExecuteDeleteAsync break (fixed in
      # #1102 and again in #1106). Block their major bumps until the project
      # deliberately migrates the runtime EF stack together.
      - dependency-name: "Microsoft.EntityFrameworkCore"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.EntityFrameworkCore.Sqlite"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.EntityFrameworkCore.Design"
        update-types: ["version-update:semver-major"]
      # Central Package Management aligns the whole backend stack to the net8.0
      # TFM (#1127, ADR-0039). The packages below were re-aligned DOWN from 9.x/10.x
      # to the 8.x line; without major-version caps the next weekly dependabot run
      # re-proposes the same majors and reverts the alignment — the exact recurrence
      # ADR-0034 documents. Block their majors until the backend deliberately moves
      # off net8.0 as one coordinated migration.
      # Microsoft.EntityFrameworkCore.Tools is now capped here too: it is design-time
      # only (PrivateAssets), but on its 10.x line it dragged EF Core 9.x in
      # transitively, so it is pinned to 8.0.x with the rest of the EF stack (ADR-0039).
      - dependency-name: "Microsoft.EntityFrameworkCore.Tools"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.AspNetCore.SignalR.Client"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.Extensions.Hosting"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.Extensions.Http.Polly"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.Extensions.Logging.Abstractions"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Npgsql.EntityFrameworkCore.PostgreSQL"
        update-types: ["version-update:semver-major"]
      # FluentAssertions 8.x+ requires a paid commercial license (Xceed); the
      # project intentionally stays on the free 7.x line (maintainer decision on
      # #1088). Block major bumps so v8 isn't re-proposed every cycle.
      - dependency-name: "FluentAssertions"
        update-types: ["version-update:semver-major"]
      # The Redis stack backs the SignalR backplane (ADR-0025) and the optional
      # Redis-backed cache service (RedisCacheService), both config-gated and DORMANT
      # in the single-instance default (Cache:Provider=InMemory, SignalR:Redis
      # connection string empty). Under the archive pivot (single-instance, SQLite,
      # personal-use) a major bump carries breaking-change risk on never-exercised
      # runtime paths with no benefit, so block majors on both direct Redis packages:
      #   - StackExchange.Redis: the underlying client (cache service + transitive).
      #   - Microsoft.AspNetCore.SignalR.StackExchangeRedis: the SignalR backplane
      #     implementation itself (direct dependency in Directory.Packages.props),
      #     otherwise its own semver-major could still reopen the dormant churn.
      # PR #1224 was closed for proposing StackExchange.Redis 3.0.0. Revisit only if
      # Redis is ever deliberately activated.
      - dependency-name: "StackExchange.Redis"
        update-types: ["version-update:semver-major"]
      - dependency-name: "Microsoft.AspNetCore.SignalR.StackExchangeRedis"
        update-types: ["version-update:semver-major"]
    commit-message:
      prefix: "deps(nuget)"

  # --- npm (frontend) ---
  - package-ecosystem: npm
    directory: /frontend/taskdeck-web
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: America/Chicago
    open-pull-requests-limit: 10
    reviewers:
      - jekyt
    labels:
      - dependencies
      - frontend
    groups:
      # Group minor/patch updates together
      npm-minor-patch:
        update-types:
          - minor
          - patch
    ignore:
      # @types/node must track the pinned Node RUNTIME major, not float ahead of
      # it. The frontend pins Node to the 24.x line (package.json engines
      # ">=24.13.1 <25", .nvmrc 24.13.1, every CI job on node-version 24.13.1).
      # @types/node's major tracks the Node major it describes, so on Node 24 the
      # correct types line is 24.x. Without this cap dependabot drifted it to 25.x
      # (one major ahead) and then proposed 26.0.0 (two ahead, PR #1230), widening
      # the gap between the typed and the executed Node API surface. This is the
      # frontend analog of the runtime-coupled NuGet caps (ADR-0034/ADR-0039):
      # block the major, let 24.x minor/patch flow. Remove only when the Node
      # runtime pin is deliberately moved to a new major (engines + .nvmrc + CI).
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
    commit-message:
      prefix: "deps(npm)"

  # --- GitHub Actions ---
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: America/Chicago
    open-pull-requests-limit: 5
    reviewers:
      - jekyt
    labels:
      - dependencies
      - ci
    groups:
      # Group all Actions updates together since they are low-risk
      actions-all:
        update-types:
          - major
          - minor
          - patch
    commit-message:
      prefix: "deps(actions)"
