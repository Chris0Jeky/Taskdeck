namespace Taskdeck.Domain.Enums;

/// <summary>
/// The bounded set of source artefacts accepted by the local intake store.
/// </summary>
public enum ArtefactKind
{
    Image = 0,
    Pdf = 1,
    TextFile = 2
}
