# v0.5 — Speak, Type, Paste, or Drop: salvaged acceleration material

Last Updated: 2026-09-02

The v0.5 bundle's GitHub grounding failed, so it carries no issue packs, queue or dependency
analysis. What survived, and where it now lives:

| File | What it is |
| --- | --- |
| `CANDIDATE_MAP.md` | the artefact-to-issue map and per-issue seeds this pass produced in place of the missing packs, validated against `main` |
| `ARCHITECTURE_SPINE.md` | the bundle's processing spine and 12 invariants in ADR-0065 vocabulary, each tied to its counterpart rule |
| `candidates/` | 8 C# / 3 TypeScript / 2 Python reference files (see its README) |
| `schemas/evaluation-fixture.schema.json` + `fixtures/` | the CF-24A `#2319` fixture-contract draft and the canary / evaluation example inputs, checked through `../contracts.manifest.json` |
| `diagrams/` | `.mmd` / `.dot` sources and `.svg` renders: milestone architecture and dependencies, multimodal lineage, processing-and-route sequence, retention reference lifecycle, presentation-profile boundary, automation safety ladder, agent integration train |
