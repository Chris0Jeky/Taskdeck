# Agent Failure Ledger

This is the human-readable view of recurring agent, tool, test, CI, and workflow failures.
Deliberately recorded raw entries live in `docs/agentic/failure_ledger.jsonl`.
Rows sharing a surface and first tracking issue in `future_fix` show only their latest state here; the JSONL retains append-only history.

## Entries

| Date | Class | Surface | Failure | Workaround | Future fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-29 | blocker | dependabot/nuget | dotnet-minor-patch repeatedly proposed EF Core 9.x while Sqlite/Design remained 8.x, causing ambiguous ExecuteDeleteAsync errors on #1102/#1106 | Pin EF Core back to 8.0.27 per PR | Resolved by #1112 ignore rule and the EF runtime 8.x pins in #760/#767 | resolved |
| 2026-05-29 | blocker | dependabot/nuget | After FluentAssertions moved to free 7.x in #1088, Dependabot immediately proposed paid v8 in #1117 | Close the v8 PR | Resolved by the FluentAssertions major ignore rule in #1118/ADR-0034 | resolved |
| 2026-05-29 | invalid_signal | ci/e2e-smoke | E2E Smoke intermittently times out at multi-board.spec.ts:197 restoredBoard visibility with a transient DB connection error unrelated to the PR diff | Investigate against the diff; rerun only the failed job when unrelated | Stabilize the archive-to-restore seed or adjust the visibility wait after root-cause proof | open |
| 2026-05-29 | non_blocking_risk | git/worktree | gh pr merge --delete-branch on a stacked base auto-closed dependent PR #1096; it could not reopen after the base disappeared | Recover the dependent on a new main-based branch/PR (#1104) | Retarget dependent PRs before deleting any stacked base branch | resolved |
| 2026-07-13 | pre_existing_noise | test/redis-lifecycle | #1298's second full backend run failed RedisCacheServiceTests.Dispose_IsNotSerialized_BehindAnInFlightConnect; the exact test passed 5/5 | Park #1298 without a PR instead of treating a narrow rerun as a green full gate | Resolved by #1392 (closes #1332) on 2026-07-17: a dedicated named background Thread replaces Task.Run so the dispose-vs-connect seam is reached deterministicall... | resolved |
| 2026-07-13 | pre_existing_noise | test/background-workers | #1334's full suite let a hosted LLM worker pre-claim a test row and a delayed presence join arrive after events were cleared | Keep the full run non-green and link exact evidence from the PR | Resolved 2026-07-17: workers half by #1394 (closes #1335) + #1391 (closes #1383); presence half by #1366 (snapshot ordering) + #1371 (phase drains). A shared Ho... | resolved |
| 2026-07-17 | non_blocking_risk | github/project-sync | Resolution record for the 2026-07-13 project-sync scope row: the gh token lacked read:project scope so Priority fields were unaudited | No workaround needed any longer | Resolved 2026-07-17 (#1327 scope item): maintainer ran gh auth refresh -s project; Sync-TaskdeckProjectPriority.ps1 audit scanned 1000 items with 0 needing sync | resolved |
| 2026-07-14 | blocker | ci/extended-workflow | Resolution record for the earlier #1330 open row: CI Extended reusable Gitleaks permission startup failures are repaired and the tracking issue is closed | No workaround remains; use exact-head Extended runs as the verification signal | Resolved by 66382e6c (Fix CI Extended Gitleaks permissions); #1330 closed 2026-07-13 | resolved |
| 2026-07-14 | blocker | ci/nightly-k6 | CI Nightly run 29229402012 and prior runs lost both k6 summary JSON files to bind-mount permission denial while the tagged SQLite board-write p95 gate failed at... | Do not treat the always-red lane as trustworthy regression evidence until summary ownership and the measured capacity contract are repaired | #1358: map both k6 containers to the host UID/GID, warn at the measured 2000ms capacity, gate at 2200ms, and prove exact-head CI. Resolved: First post-#1359 nig... | resolved |
| 2026-07-25 | non_blocking_risk | dependency/sqlite-native | Resolution record: the inherited HIGH SQLite native advisory is removed from Taskdeck's resolved dependency graph | No workaround remains; keep the runtime security-floor regression and dependency audit green | #1345 resolved by centrally pinning the matched SQLitePCLRaw bundle/core/provider/native family at 2.1.12; SQLite 3.53.3 loads, the >= 3.50.2 floor test passes,... | resolved |
| 2026-07-26 | blocker | github/project-sync | Correction to the 2026-07-17 resolution record: Sync-TaskdeckProjectPriority.ps1 stopped at 1,000 items and reported 0 updates, but the project had more items; ... | Treat every positive -Limit as a fail-closed ceiling and require a stable complete read-only audit; do not run -Apply while invalid Issue labels or cross-reposi... | #1458 / draft PR #1488 implements cursor-complete stable snapshots and fail-closed derivation; keep this open until truthful labels, cross-repository policy, re... | open |
| 2026-07-26 | pre_existing_noise | frontend/workspace-mode-ordering | Resolution record for the #1343 preference-vs-summary ordering defect; the original observed failure remains in append-only history | No workaround remains for the core stale-summary overwrite; retain separate residual debt instead of overstating closure | #1343 core resolved by PR #1386 merge dba47320e348b80ecb678c41a51b686c954527ee; separate server-order/cross-device and client-state residuals remain #1388/#1410... | resolved |
| 2026-07-26 | blocker | frontend/paper-review-contract | Resolution record for the #1347 numeric deep-review enum wire-contract crash; the original observed failure remains in append-only history | No workaround remains; valid numeric payloads render directly and unexpected values fail closed to visible warning states | #1347 resolved by PR #1360 merge ed2538ffb0388523882238653b34fcc6f431a512: API numeric-enum serialization, frontend mapping, conservative malformed-value handli... | resolved |
| 2026-07-26 | blocker | backend/similar-past | Resolution record for the #1348 SQLite-backed similar-past HTTP 500; the original observed failure remains in append-only history | No workaround remains; keep the bounded board/user scope and real SQLite/browser coverage as regression evidence | #1348 resolved by PR #1361 merge ccd7bf93721c07171099383c77bfb508c23fbf48; Paper-browser completion by PR #1362 merge 40b7bb2f3819dcc7d0bd6349a8f516fd3169190f. ... | resolved |
| 2026-07-26 | non_blocking_risk | agent/tool-command-composition | Resolution record for the repeated #1490 PowerShell/native command-composition failures; the original recurrence remains in append-only history | No workaround remains; use the copyable safe forms and classify future failures through the ledger process | #1490 resolved by PR #1491 merge 61f718af941c56c31b8b7595333b9debd6e47da8: the MCP tooling guide now covers collected foreach output, optional rg exits, safe mu... | resolved |
| 2026-07-27 | non_blocking_risk | ci/mutation-testing-backend | Resolution record for the 16-run backend mutation evidence outage; the original failures remain in append-only history. | No workaround remains for producing bounded Domain mutation evidence; retain the non-blocking score posture while collecting the policy's 3-4-run calibration wi... | #1500 / PR #1503 repaired the lane at exact workflow head 307add004fbe142321a6ec11be21fab708824d5d. Backend-only run 30236307062 completed in 192 seconds with a... | resolved |
| 2026-07-27 | pre_existing_noise | test/sqlite-concurrency | Correction to the #1373 resolution record: Required API Integration (windows-latest) on PR #1505 source head 6ee17a4849427d679722a9d1e034d4622536f4fe returned H... | Keep every recurrence red and use the bounded #1512 correlation/exception-classification diagnostics to capture the first server-side cause; do not retry the re... | #1373 resolved the proven #1282 SQLite registration-parity cause; successor #1512 remains open after its diagnostic-only slice until a recurrence identifies the... | open |
| 2026-07-27 | blocker | test/testcontainers-dockerless-skip | Taskdeck.Integration.Tests constructed its PostgreSQL Testcontainer in the collection-fixture constructor, so Testcontainers validated an unavailable Docker end... | Run the focused non-container projects while #1518 is in review; do not retry a known-hanging Docker probe merely to obtain a local full-suite verdict | #1518 defers PostgreSqlBuilder.Build until after DockerAvailableCheck succeeds, proves the unavailable path never invokes the factory, and retains Docker-backed... | resolved |
| 2026-07-31 | blocker | agent/git-transport-lifecycle | A native post-merge Git pull exceeded the shell deadline and left its Git fetch and remote-HTTPS descendants alive; later GitHub operations also hit TLS handsha... | Disable interactive prompts, bypass the credential helper, apply Git HTTP low-speed bounds, fetch the exact main ref, then fast-forward locally; identity-check ... | Follow-up to #1498 bounds both Git launch paths in the issue-worktree helper, reaps a timed-out process tree, and proves a real remote-helper child is absent be... | resolved |
| 2026-07-31 | blocker | agent/python-launcher | Resolution record for the repeated native-Windows Store-alias launcher failure; the original observed failures remain in append-only history | No workaround remains for Taskdeck utilities: native Windows uses py -3 -B and POSIX uses python3 -B; runtime handlers were later retired | #1487 resolved by merged PR #1496; #1552 retains the verified launchers only for the manual failure-ledger renderer and synchronization test, while agent-harnes... | resolved |
| 2026-07-31 | non_blocking_risk | agent/runtime-hook-duplication | The Taskdeck Claude project floor duplicated the global PreToolUse dispatcher, denied six of eight benign raw-text probes, left native PowerShell outside its cl... | No local-hook workaround remains; record real failures deliberately and distinguish project hook absence from surviving user, organization, or runtime controls | #1552 retires Taskdeck-owned runtime hooks and local deny rules while preserving T3 authority; agent-harness#188 and #189 own reusable retirement and duplicate-... | resolved |
| 2026-07-31 | blocker | agent/powershell-deny-floor | Resolution record for the Taskdeck-local deny floor's incomplete native-PowerShell coverage; the original observed failure and proposed expansion remain in appe... | No Taskdeck-local deny floor remains; irreversible global laws, T3 authority, CI, worktree guards, and separately inventoried external control layers still appl... | #1497 is superseded by #1552, which retires the incomplete local floor instead of expanding it; agent-harness#188 and #189 own reusable retirement and duplicate... | resolved |
| 2026-08-01 | non_blocking_risk | test/migration-validation-git-bash-windows | scripts/ci/validate-migrations.sh passed its POSIX /tmp SQLite database path literally to the Windows dotnet process under Git Bash, causing SQLite Error 14 bef... | Use the native-Windows fresh-database dotnet ef database update, MigrationBootstrapTests, and has-pending-model-changes checks locally; require the hosted Linux... | Teach the migration validation script to translate Git-for-Windows paths at the process boundary or route native Windows through an equivalent provider-specific... | open |
| 2026-08-09 | blocker | ci/extended-workflow | Resolution record for the two pre-checkout Docker Hub Actionlint bootstrap failures; the original hosted failure evidence remains in append-only history | No workaround remains: CI Extended now uses the checksum-verified direct Actionlint/Pyflakes bootstrap with explicit runner ShellCheck and focused contracts | #1510 resolved by recovered PR #1511 merge e989e3e0 without rewriting or discarding its ancestry; exact-head CI 31333250323, CI Extended 31333250401, and CodeQL... | resolved |
| 2026-07-27 | blocker | ci/staging-gate-rehearsal | The manual staging workflow supplied only TASKDECK_JWT_SECRET while deploy/docker-compose.yml also requires TASKDECK_CONNECTORS_ENCRYPTION_KEY, so Compose confi... | Do not dispatch or call the staging path runnable from main; use no production credentials and keep deployment inactive while the stacked fix is reviewed | #1506: generate and mask per-job JWT and connector inputs, fail closed on required-variable drift, remove the environment binding, then require hosted non-deplo... | open |
| 2026-08-16 | blocker | github/project-sync | Resolution record for the ProjectV2 mixed-snapshot incident; all historical failure rows remain in append-only history | No workaround remains: recognized count/stamp drift uses bounded whole-snapshot restart; malformed pagination and intrinsic overflow/premature-count states fail... | #1647 resolved by merges a76cca06, 226786ce, and bbe578bd; authentication-free checks passed 102/102 | resolved |
| 2026-08-16 | blocker | ci/windows-cli-process-lifecycle | Resolution record for the Windows CLI process-lifecycle runway; all historical failure rows remain in append-only history | No workaround remains: every real-process contract uses the shared bounded launcher with a derived 60-second deadline; default harnesses use isolated bytes from... | #1530 resolved by merges c51b3170 and a4c2ac80; exact-head Windows evidence passed 127/127 in 2m28s; #1669 recurrence is included in the historical rows | resolved |
| 2026-08-18 | blocker | agent/read-only-inventory-artifacts | Two read-only inventory lanes wrote a generic GitHub snapshot into the primary checkout; the second lane overwrote and then deleted a pre-existing untracked .tm... | Feature work paused; the 122-issue logical snapshot was reconstructed from the same live query with the later #1706 item excluded, but byte-for-byte identity ca... | #1707 updates both batch-orchestration skills to require in-memory or unique system-temp snapshots, provenance-gated cleanup, and coordinator status comparison | resolved |
| 2026-08-18 | blocker | agent/read-only-inventory-enforcement | After the procedural no-checkout-write repair merged, an explicitly read-only PR monitor still redirected two GitHub snapshots into the primary checkout, provin... | Feature work paused; exact agent provenance identified the two identical current-run status files, and only those owned paths were removed while the preserved c... | #1709 adds a bounded authenticated pre/post fingerprint gate for delegated read-only waves, detecting overwrite, deletion, and creation without reading ignored ... | resolved |
| 2026-08-18 | invalid_signal | frontend/vitest-snapshot-status | Repeated Windows Vitest snapshot checks reported a false dirty status because Git's index stat cache held a different cached size while the snapshot worktree by... | Before any index refresh, prove one exact snapshot path, that it is a non-directory, hash-object equals rev-parse :path, ordinary git diff is empty, and zero sn... | #1714: document and automate bounded Windows Vitest snapshot false-dirty status classification | open |
| 2026-08-18 | blocker | ci/playwright-bootstrap-dependencies | Correction to the #1747 resolution record: the original 30-minute candidate remains in append-only history, while the reviewed and shipped Required E2E Smoke wo... | No workaround remains: keep the named fail-closed phase bounds and use exact job-step evidence to distinguish dependency bootstrap stalls from product-test fail... | #1747 / PR #1748 shipped a 35-minute outer job ceiling with 10-minute OS-dependency, 5-minute pinned-Chromium, and 12-minute smoke bounds, plus an executable Wo... | resolved |
| 2026-08-18 | blocker | agent/read-only-github-mutation | A delegated lane explicitly briefed as read-only posted and then deleted a no-op comment on historical PR #1; after the separate filesystem mutation tracked by ... | Feature work paused; the coordinator verified the comment was absent through the API and suspended unconstrained delegated GitHub inventory commands | #1753 adds an allowlisted argv wrapper for delegated shell-backed Git/GitHub inventory and routes both batch-orchestration skills and the autonomy runbook throu... | resolved |
| 2026-08-21 | non_blocking_risk | frontend/vitest-dependencies | The focused frontend Vitest command could not load the worktree config because vitest/config and @vitejs/plugin-vue are absent from the installed dependency tre... | Validated the docs wrappers with PowerShell and Bash parsers, validated demo-director arguments, and ran the dependency-free demo:run -- --list check; do not cl... | Restore the frontend npm dependency install in the issue worktree before relying on targeted Vitest evidence | open |

## Classification

- `blocker`: work cannot safely continue.
- `non_blocking_risk`: work can continue, but verification confidence is reduced.
- `pre_existing_noise`: unrelated existing failure that should still be visible.
- `invalid_signal`: false alarm, stale check, or non-applicable warning.

## Promotion Rule

A ledger entry should become a guide or skill update only when it is reproducible, project-specific, and likely to recur.
Use `docs/agentic/GUIDE_UPDATE_PROTOCOL.md`; do not mutate root instructions after a single ambiguous failure.
